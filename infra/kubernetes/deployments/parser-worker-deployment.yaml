apiVersion: apps/v1
kind: Deployment
metadata:
  name: parser-worker
  namespace: ai-finance
  labels:
    app: parser-worker
    version: v1
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: parser-worker
  template:
    metadata:
      labels:
        app: parser-worker
        version: v1
    spec:
      serviceAccountName: app-sa
      containers:
      - name: parser-worker
        image: 014071048720.dkr.ecr.us-east-1.amazonaws.com/ai-finance/parser-worker:latest
        imagePullPolicy: Always
        ports:
        - name: http
          containerPort: 3003
          protocol: TCP
        
        envFrom:
        - configMapRef:
            name: app-config-dev
        - configMapRef:
            name: queue-config
        - secretRef:
            name: app-secrets
        
        env:
        - name: SERVICE_NAME
          value: "parser-worker"
        
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        
        # Health checks - Commented out until /health endpoint is implemented
        # livenessProbe:
        #   httpGet:
        #     path: /health
        #     port: 3003
        #   initialDelaySeconds: 30
        #   periodSeconds: 10
        #   timeoutSeconds: 3
        #   failureThreshold: 3
        # 
        # readinessProbe:
        #   httpGet:
        #     path: /health
        #     port: 3003
        #   initialDelaySeconds: 10
        #   periodSeconds: 5
        #   timeoutSeconds: 3
        #   failureThreshold: 3
        
        securityContext:
          runAsNonRoot: true
          runAsUser: 1001
          readOnlyRootFilesystem: false
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
        
        volumeMounts:
        - name: tmp
          mountPath: /tmp
      
      volumes:
      - name: tmp
        emptyDir: {}
      
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - parser-worker
              topologyKey: kubernetes.io/hostname