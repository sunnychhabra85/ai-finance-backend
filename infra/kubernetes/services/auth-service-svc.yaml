apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: ai-finance
  labels:
    app: auth-service
spec:
  type: ClusterIP  # Internal service only
  ports:
  - name: http
    port: 3001
    targetPort: 3001
    protocol: TCP
  selector:
    app: auth-service